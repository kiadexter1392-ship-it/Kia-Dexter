The repo is essentially empty (just an old unrelated `index.html`). I'll build both sites from scratch: a real Node + SQLite backend (so registrations persist), a coach panel, and a members' panel. Let me start with the shared core.



Now the coach site's backend:



Now the shared design system (CSS + browser JS helpers):



Let me smoke-test the backend before building the front-ends: